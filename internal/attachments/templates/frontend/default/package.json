{
  "name": "project",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build:dev": "npx --yes parcel build --no-source-maps --no-optimize --dist-dir ./static ./assets/styles.css",
    "build:prod": "npx --yes parcel build --no-source-maps --dist-dir ./static ./assets/styles.css"
  },
  "license": "MIT",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "devDependencies": {
    "parcel": "latest"
  }
}
