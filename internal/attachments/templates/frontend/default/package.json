{
  "name": "project",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build:dev": "npx parcel build --dist-dir ./static ./assets/styles.css --no-source-maps --no-optimize",
    "build:prod": "npx parcel build --dist-dir ./static ./assets/styles.css --no-source-maps"
  },
  "license": "MIT",
  "devDependencies": {
    "parcel": "latest"
  }
}
