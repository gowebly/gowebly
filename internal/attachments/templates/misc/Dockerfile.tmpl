# Auto generated Dockerfile file. DO NOT EDIT!

FROM golang:1.21-alpine AS builder

# Move to working directory (/build).
WORKDIR /build

# Copy and download dependency using go mod.
COPY go.mod go.sum ./
RUN go mod download

# Copy your code into the container.
COPY . .

# Set necessary environment variables and build your project.
ENV CGO_ENABLED=0 GOOS=linux GOARCH=amd64 BACKEND_PORT={{ .Port }} BACKEND_READ_TIMEOUT={{ .Timeout.Read }} BACKEND_WRITE_TIMEOUT={{ .Timeout.Write }}
RUN go build -ldflags="-s -w" -o project ./...

FROM scratch

# Copy project's binary from /build to the scratch container.
COPY --from=builder ["/build/project", "/"]

# Exposing container port.
EXPOSE {{ .Port }}

# Set entry point.
ENTRYPOINT ["/project"]
